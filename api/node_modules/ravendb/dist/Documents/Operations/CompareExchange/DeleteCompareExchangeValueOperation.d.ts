import { HttpRequestBase } from '../../../Primitives/Http';
import { IOperation, OperationResultType } from "../OperationAbstractions";
import { CompareExchangeResult } from "./CompareExchangeResult";
import { ClassConstructor } from "../../../Types";
import { IDocumentStore } from "../../IDocumentStore";
import { DocumentConventions } from "../../Conventions/DocumentConventions";
import { HttpCache } from "../../../Http/HttpCache";
import { RavenCommand } from "../../../Http/RavenCommand";
import { ServerNode } from '../../../Http/ServerNode';
export declare class DeleteCompareExchangeValueOperation<T> implements IOperation<CompareExchangeResult<T>> {
    private _key;
    private _index;
    private _clazz;
    constructor(key: string, index: number, clazz?: ClassConstructor<T>);
    getCommand(store: IDocumentStore, conventions: DocumentConventions, cache: HttpCache): RavenCommand<CompareExchangeResult<T>>;
    readonly resultType: OperationResultType;
}
export declare class RemoveCompareExchangeCommand<T> extends RavenCommand<CompareExchangeResult<T>> {
    private _key;
    private _index;
    private _clazz;
    private _conventions;
    constructor(key: string, index: number, conventions: DocumentConventions, clazz?: ClassConstructor<T>);
    readonly isReadRequest: boolean;
    createRequest(node: ServerNode): HttpRequestBase;
    setResponse(response: string, fromCache: boolean): void;
}
