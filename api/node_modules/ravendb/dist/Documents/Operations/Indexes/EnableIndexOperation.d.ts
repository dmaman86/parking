import { IMaintenanceOperation, OperationResultType } from "../OperationAbstractions";
import { DocumentConventions } from "../../Conventions/DocumentConventions";
import { RavenCommand, ServerNode } from "../../..";
import { HttpRequestBase } from "../../../Primitives/Http";
export declare class EnableIndexOperation implements IMaintenanceOperation<void> {
    private _indexName;
    constructor(indexName: string);
    getCommand(conventions: DocumentConventions): EnableIndexCommand;
    readonly resultType: OperationResultType;
}
export declare class EnableIndexCommand extends RavenCommand<void> {
    private _indexName;
    constructor(indexName: string);
    createRequest(node: ServerNode): HttpRequestBase;
    readonly isReadRequest: boolean;
}
