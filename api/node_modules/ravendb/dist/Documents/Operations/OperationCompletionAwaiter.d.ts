import { RequestExecutor, DocumentConventions } from "../..";
import { RavenCommand, IRavenResponse } from "../../Http/RavenCommand";
export declare class OperationCompletionAwaiter {
    private _requestExecutor;
    private _conventions;
    private _id;
    readonly id: number;
    constructor(requestExecutor: RequestExecutor, conventions: DocumentConventions, id: number);
    private _fetchOperationStatus();
    protected _getOperationStateCommand(conventions: DocumentConventions, id: number): RavenCommand<IRavenResponse>;
    waitForCompletion(): Promise<void>;
}
