import { InMemoryDocumentSessionOperations } from "./InMemoryDocumentSessionOperations";
import { DocumentInfo } from "./DocumentInfo";
import { DocumentConventions } from "../Conventions/DocumentConventions";
import { DocumentType } from "../DocumentAbstractions";
export declare class EntityToJson {
    private _session;
    private _jsonParser;
    constructor(session: InMemoryDocumentSessionOperations);
    private _missingDictionary;
    readonly missingDictionary: Map<object, Map<string, object>>;
    convertEntityToJson(entity: object, documentInfo: DocumentInfo): object;
    static convertEntityToJson(entity: object, conventions: DocumentConventions): object;
    private static _writeMetadata(jsonNode, typeInfo, documentInfo);
    convertToEntity(targetEntityType: DocumentType, id: string, document: object): object;
    private static _getEntityTypeInfoFromMetadata(document);
    private static _tryRemoveIdentityProperty(document, entityType, conventions);
}
