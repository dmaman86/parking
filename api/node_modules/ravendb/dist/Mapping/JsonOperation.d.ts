import { DocumentInfo } from "../Documents/Session/DocumentInfo";
import { DocumentsChanges } from "../Documents/Session/DocumentsChanges";
export declare class JsonOperation {
    static entityChanged(newObj: object, documentInfo: DocumentInfo, changes: {
        [id: string]: DocumentsChanges[];
    }): boolean;
    private static _compareJson(id, originalJson, newJson, changes, docChanges);
    private static _compareValues(oldProp, newProp);
    private static _compareJsonArray(id, oldArray, newArray, changes, docChanges, propName);
    private static _newChange(name, newValue, oldValue, docChanges, change);
}
